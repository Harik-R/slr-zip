/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  }
    module.exports = {
  experimental: {
  appDir: true,
},
};

module.exports = nextConfig;
